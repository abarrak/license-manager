<% provide :title, @license.title %>

<p style="color: green"><%= notice %></p>

<%= render @license %>

<div>
  <%= link_to "Edit 📝", edit_license_path(@license) %> |
  <%= link_to "Back ⮐", licenses_path %>

  <div class="text-right">
    <%= button_to "Destroy 🗑", @license, method: :delete, form: { data: { turbo_confirm: 'Are you sure?' } } %>
  </div>
</div>
